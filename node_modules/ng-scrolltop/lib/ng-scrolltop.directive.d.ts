import { NgZone } from '@angular/core';
import { NgScrolltopService } from './ng-scrolltop.service';
export declare class NgScrolltopDirective {
    service: NgScrolltopService;
    private ngZone;
    startTime: number;
    startTop: number;
    progress: number;
    constructor(service: NgScrolltopService, ngZone: NgZone);
    onclick(): void;
    easing: (x: any) => number;
    scrollABit(timestamp: any): void;
}
